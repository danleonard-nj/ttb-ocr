apiVersion: v1
kind: Service
metadata:
  name: {{ include "treasury-assignment.fullname" . }}
  labels:
    {{- include "treasury-assignment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "treasury-assignment.selectorLabels" . | nindent 4 }}

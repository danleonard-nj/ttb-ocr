apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "treasury-assignment.fullname" . }}-test-connection"
  labels:
    {{- include "treasury-assignment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "treasury-assignment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
